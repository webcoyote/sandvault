#!/usr/bin/env bash
set -Eeuo pipefail

if [[ ! -x "$HOME/node_modules/bin/gemini" ]]; then
    echo >&2 "Installing gemini..."
    npm install --global --silent @google/gemini-cli
fi

exec "$HOME/node_modules/bin/gemini" "$@"
